export const Button = ({children}) => {
  return (
    <button className="mt-12 py-2 px-5 rounded-lg text-bold text-white bg-gradient-to-r from-green-400 to-blue-500 hover:from-pink-500 hover:to-yellow-500">{children}</button>
  )
}
